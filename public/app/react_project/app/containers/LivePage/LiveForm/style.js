import styled from 'styled-components';
import { rem } from 'utils/style';
import { TextareaRedux } from 'components/Form/ReduxForm';

export const StyledTextarea = styled(TextareaRedux)`
    height: ${rem(110)};
`;
