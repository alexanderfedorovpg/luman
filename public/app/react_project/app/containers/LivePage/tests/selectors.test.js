// import { fromJS } from 'immutable';
// import { makeSelectLivePageDomain } from '../selectors';

// const selector = makeSelectLivePageDomain();

describe('makeSelectLivePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
