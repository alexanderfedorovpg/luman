// import { fromJS } from 'immutable';
// import { makeSelectProgramsPageDomain } from '../selectors';

// const selector = makeSelectProgramsPageDomain();

describe('makeSelectProgramsPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
