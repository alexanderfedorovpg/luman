// import { fromJS } from 'immutable';
// import { makeSelectEditionPageDomain } from '../selectors';

// const selector = makeSelectEditionPageDomain();

describe('makeSelectEditionPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
