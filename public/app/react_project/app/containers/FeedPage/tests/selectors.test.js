// import { fromJS } from 'immutable';
// import { makeSelectFeedPageDomain } from '../selectors';

// const selector = makeSelectFeedPageDomain();

describe('makeSelectFeedPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
