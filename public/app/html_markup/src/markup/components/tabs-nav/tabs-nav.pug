mixin tabs-nav(data)
    +b.tabs-nav()&attributes(attributes)
        //- +e.A.item.is-active(href=jv0) Сегодня
        each item in data.items
            //- div
            +e.A.item(href=jv0 class= item.class)= item.name